# Пример: AWS EC2 + Docker для База
# Переименовать в aws.tf и настроить

# data "aws_ami" "ubuntu" {
#   most_recent = true
#   owners      = ["amazon"]
#   filter {
#     name   = "name"
#     values = ["ubuntu/images/hvm-ssd/ubuntu-jammy-22.04-amd64-server-*"]
#   }
# }
#
# resource "aws_instance" "baza" {
#   ami           = data.aws_ami.ubuntu.id
#   instance_type = "t3.medium"  # 4 vCPU, 8 GB для 250 пользователей — t3.large
#   key_name      = aws_key_pair.baza.key_name
#   vpc_security_group_ids = [aws_security_group.baza.id]
#   user_data = templatefile("${path.module}/cloud-init.yaml", {})
#   tags = { Name = "baza" }
# }
#
# resource "aws_security_group" "baza" {
#   ingress {
#     from_port   = 443
#     to_port     = 443
#     protocol    = "tcp"
#     cidr_blocks = ["0.0.0.0/0"]
#   }
#   ingress {
#     from_port   = 80
#     to_port     = 80
#     protocol    = "tcp"
#     cidr_blocks = ["0.0.0.0/0"]
#   }
#   egress {
#     from_port   = 0
#     to_port     = 0
#     protocol    = "-1"
#     cidr_blocks = ["0.0.0.0/0"]
#   }
# }
